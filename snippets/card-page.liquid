<div class="card card--border card-animate p-s rad-m o-hidden d-flex fd-column h-100 w-100">
  <a href="{{ page.url }}" aria-label="{{ page.title }}" class="c-pointer d-grid gap-2xs ta-left">
    <span class="tag tt-up maw-fit-content">{{ 'general.search.page' | t }}</span>
    <span class="fs-heading-m component-heading hide-empty">
      {{- page.title | truncate: 50 | escape -}}
    </span>
  </a>
</div>