<div class="card card--border card-animate p-xs rad-m o-hidden d-flex fd-column h-100 w-100">
  <a href="{{ page.url }}" aria-label="{{ page.title }}" class="c-pointer d-grid gap-3xs ta-left ele-focus-visible">
    <span class="tag tt-up maw-fit-content">{{ 'general.search.page' | t }}</span>
    <span class="fs-heading-s component-heading hide-empty">
      {{- page.title | escape -}}
    </span>
  </a>
</div>